spring:
  application:
    name: frameworks
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/hmdp?serverTimezone=UTC
    username: root
    password: root
